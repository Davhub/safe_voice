import 'package:flutter/material.dart';

class AppColors {
  // Primary color
  static const Color primary = Color(0xFFFE6501); // #fe6501

  // Secondary color (complementary to primary)
  static const Color secondary = Color(0xFF015AFE); // #015afe

  // Accent color (for highlights, buttons)
  static const Color accent = Color(0xFFFFC107); // Amber

  // Backgrounds
  static const Color background = Color(0xFFF0F0FF); // Soft light background
  static const Color card = Colors.white;

  // Text colors
  static const Color textPrimary = Color(0xFF2C2C54); // Dark blue
  static const Color textSecondary = Color(0xFF757575); // Grey
  static const Color textOnPrimary = Colors.white;

  // Error
  static const Color error = Color(0xFFD32F2F); // Red

  // Success
  static const Color success = Color(0xFF2ECC71); // Green

  // Info
  static const Color info = Color(0xFF3498DB); // Blue

  // Warning
  static const Color warning = Color(0xFFFFA726); // Orange

  // Disabled
  static const Color disabled = Color(0xFFBDBDBD);

  // Divider
  static const Color divider = Color(0xFFE0E0E0);
}
